# Testing GraphQL Document Features

This page tests hover tooltips for GraphQL documents.

## Basic Query

Hover over the identifiers below to see tooltips.

```graphql
query GetPokemon {
  pokemon(id: "1") {
    id
    name
    types
  }
}
```

## Query with Variables

Test queries with variables:

```graphql
query GetPokemonByName($name: String!) {
  pokemon(name: $name) {
    id
    name
    height
    weight
  }
}
```

## Fragment Example

Complex query with fragments:

```graphql
fragment PokemonDetails on Pokemon {
  id
  name
  types
  height
  weight
}

query GetMultiplePokemon {
  pokemon1: pokemon(id: "1") {
    ...PokemonDetails
  }
  pokemon2: pokemon(id: "4") {
    ...PokemonDetails
  }
}
```

## Mutation Example

```graphql
mutation UpdatePokemon($id: ID!, $name: String!) {
  updatePokemon(id: $id, name: $name) {
    id
    name
    updatedAt
  }
}
```

## Subscription Example

```graphql
subscription OnPokemonUpdate($id: ID!) {
  pokemonUpdated(id: $id) {
    id
    name
    types
    stats {
      hp
      attack
      defense
    }
  }
}
```