# Get a trainer with their Pokemon team
query GetTrainer($id: ID!) {
  trainer(id: $id) {
    id
    name
    hometown
    badges
    team {
      id
      name
      types
      level
      stats {
        hp
        attack
        defense
        speed
      }
    }
  }
}